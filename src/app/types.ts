export enum ROUTES {
  LOGIN = "/login",
  REGISTER = "/register",
  RESET_PASSWORD = "/reset-password",
  VERIFICATION_LINK = "/verification-link",
  DASHBOARD = "/dashboard",
  PROFILE = "/profile",
  CHECKIN = "/check-in",
  PROGRESS = "/progress",
}
